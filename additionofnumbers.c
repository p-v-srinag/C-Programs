#include<stdio.h>
int main()
{
   int x,y,a;
   printf("Enter the first Number:");
   scanf("%d",&x);
   printf("Enter the second Number:");
   scanf("%d",&y);
   a=x+y;
   printf("%d",a);
    return 0;
}