#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter the first number:");
    scanf("%d",&a);
    printf("Enter the second Number:");
    scanf("%d",&b);
    c=a*b;
    printf(" The product of two numbers = %d",c);
    return 0;
}